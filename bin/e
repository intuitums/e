#!/bin/sh
# e launcher. Runs the TypeScript entry directly on Node's type stripping.
exec node --experimental-strip-types --no-warnings "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")/../src/main.ts" "$@"
