//! Footer surfaces: the shared panel frame, the picker, settings, auth, and
//! trust, plus the side pane extensions open. Every surface frames through
//! `panel` so the look can't diverge.

pub mod authpanel;
pub mod menu;
pub mod pane;
pub mod panel;
pub mod settingspanel;
pub mod trustpanel;
