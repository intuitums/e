name: CI

on:
  push:
    branches: [main]
  pull_request:

# Read-only by default; no job in this repo needs write access to anything.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    # Docs-only changes can't affect the binary or the test suite, so they
    # skip the heavy jobs — but the required checks must still report green,
    # so Build & Test and Performance stay running and short-circuit their
    # steps instead of skipping themselves.
    name: Decide
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      build: ${{ steps.filter.outputs.build }}
      packages: ${{ steps.filter.outputs.packages }}
      channels: ${{ steps.filter.outputs.channels }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The push path diffs the squash commit against its parent.
          fetch-depth: 2

      - name: Gate
        id: filter
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          set -eu
          # On a pull request the file list comes from the API; on a push —
          # the squash merge itself — from the diff against the parent.
          # A listing failure is fail-open: the heavy jobs run. CI is never
          # skipped on uncertainty.
          if [ -n "$PR" ]; then
            if ! files=$(gh api "repos/$REPO/pulls/$PR/files" --paginate --jq '.[] | (.filename, .previous_filename // empty)'); then
              echo "file listing failed; running the heavy jobs"
              echo "build=true" >> "$GITHUB_OUTPUT"
              echo "packages=true" >> "$GITHUB_OUTPUT"
              echo "channels=true" >> "$GITHUB_OUTPUT"
              echo "docs=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          else
            files=$(git diff --name-only HEAD^ HEAD)
          fi
          # Paths that cannot affect the binary or the tests: prose, record
          # files, repository metadata. Anything unlisted runs the heavy
          # jobs — an unfamiliar path errs toward testing, not skipping.
          build=false
          packages=false
          channels=false
          docs=false
          while IFS= read -r file; do
            # The reference channels have their own checks.
            case "$file" in
              channels/* | tests/fixtures/channels/* | .github/workflows/ci.yml) channels=true ;;
            esac
            # Installer jobs cover package generation, launchers, and ownership.
            case "$file" in
              packaging/* | scripts/packaging/* | scripts/release/* | build.rs | install.sh | LICENSE | \
              src/core/update.rs | tests/update.rs | scripts/release-check.sh | \
              .github/workflows/ci.yml | .github/workflows/release.yml | .github/workflows/preview.yml)
                packages=true ;;
            esac
            # The guides are published to the website and embedded in the
            # binary, so a docs-only change still runs their contract in the
            # Build job below instead of skipping everything.
            case "$file" in
              docs/* | contributing/* | README.md | CONTRIBUTING.md | AGENTS.md | \
              CLAUDE.md | SECURITY.md) docs=true ;;
            esac
            case "$file" in
              *.md | docs/* | LICENSE | CHANGELOG.md | \
              benchmarks/results/* | .gitignore | .gitattributes | .editorconfig | \
              .github/pull_request_template.md | .github/CODEOWNERS | \
              .github/dependabot.yml | channels/*) ;;
              *) build=true ;;
            esac
          done <<EOF
          $files
          EOF
          echo "build=$build" >> "$GITHUB_OUTPUT"
          echo "packages=$packages" >> "$GITHUB_OUTPUT"
          echo "channels=$channels" >> "$GITHUB_OUTPUT"
          echo "docs=$docs" >> "$GITHUB_OUTPUT"

  guard:
    # Enforce fast contributor checks on every PR, including docs-only changes.
    name: Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 2

      - name: Check changed content
        run: python3 scripts/hooks/check.py HEAD^

      - name: Test contributor hooks
        run: python3 -m unittest discover -s scripts/hooks -p 'test_*.py'

      - name: Audit
        run: ./scripts/guard.sh

  test:
    # Run the repository contract on fresh GitHub-hosted Linux and macOS VMs.
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    needs: changes
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Toolchain
        if: needs.changes.outputs.build == 'true'
        run: rustc --version

      - name: Check
        if: needs.changes.outputs.build == 'true'
        run: ./x check

      - name: Docs contract
        # `./x check` runs this whenever the binary changes. A docs-only change
        # skips that, and the guides are still published and embedded, so the
        # contract is checked here — front matter, one group README per folder,
        # unique topics, and every relative link resolving.
        if: needs.changes.outputs.docs == 'true' && needs.changes.outputs.build != 'true' && runner.os == 'Linux'
        run: cargo test --locked --test docs

      - name: Frames
        if: needs.changes.outputs.build == 'true'
        run: ./x ui --out "$RUNNER_TEMP/e-ui-frames"

      - name: Retain frames
        if: failure()
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ui-frames-${{ runner.os }}
          path: ${{ runner.temp }}/e-ui-frames
          if-no-files-found: ignore
          retention-days: 7

  performance:
    name: Budgets
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Toolchain
        if: needs.changes.outputs.build == 'true'
        run: rustc --version

      - name: Bench
        if: needs.changes.outputs.build == 'true'
        run: ./x bench

      # A reviewer convenience, not a contract: GitHub's artifact service
      # occasionally 403s at finalize, and that flake must not fail the job.
      - name: Upload binary
        if: github.event_name == 'pull_request' && needs.changes.outputs.build == 'true'
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e-${{ runner.os }}-${{ runner.arch }}-${{ github.event.pull_request.number }}
          path: target/release/e
          if-no-files-found: error
          retention-days: 7

  packages:
    needs: changes
    if: needs.changes.outputs.packages == 'true'
    # A skipped matrix is not expanded, so keep the check name static.
    name: Installers
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: '1.4.2'
      - name: Verify package contents and native launchers
        run: |
          python3 -m unittest discover -s scripts/packaging -p 'test_*.py'
          node --test scripts/packaging/publish-npm.test.mjs
          scripts/packaging/smoke.sh

  channels:
    needs: changes
    if: needs.changes.outputs.channels == 'true'
    name: Channels
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
      - name: Check the Slack channel
        working-directory: channels/slack
        run: |
          npm ci
          npm run typecheck
          npm test
      - name: Check the GitHub channel
        run: python3 -m unittest discover -s channels/github -p 'test_*.py'

  container:
    needs: changes
    if: needs.changes.outputs.channels == 'true'
    name: Container
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # The image installs the released e and this commit's bot, which is what
      # an operator gets; nothing else builds it before a server runs it.
      - name: Build the Slack image
        run: docker build --tag e-slack channels/slack
